node_modules/
dist/
.astro/

# WebP siblings of public/images/*.jpg|png -- regenerated at every build by
# the `prebuild` script (scripts/to-webp.mjs), not source. Matches how the
# Canada blog handles it.
public/images/*.webp

out/
out-uk/
.env
.env.*
!.env.example

.vercel
.env*
!.env.example

# Ebook PDFs mirrored here from the digital-products build — not referenced
# by any site source or public/, not served live; kept on disk, not in git
# history (5-14MB each, was driving .git to 703MB)
dig prod/pdf/*.pdf
